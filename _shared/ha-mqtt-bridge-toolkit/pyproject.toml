[project]
name = "ha-mqtt-bridge-toolkit"
version = "0.5.0"
description = "Shared utilities for MQTT → Home Assistant bridges: HA Discovery payload builders, topic helpers, ISO-8601 time helpers, disk-backed JSONL outbox, threaded paho-mqtt v5 publisher, aiomqtt kwargs builder, YAML+env-var config loader."
requires-python = ">=3.9"
dependencies = [
    "paho-mqtt>=2.1.0",
]

[project.optional-dependencies]
# Bridges that use the YAML+${ENV_VAR} config pattern (macOS) opt in
# to PyYAML via this extra. The aiomqtt-side bridges don't need it.
yaml = [
    "pyyaml>=6.0.1",
]
dev = [
    "pytest>=8.0.0",
    "pyyaml>=6.0.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["ha_mqtt_bridge"]

[tool.pytest.ini_options]
testpaths = ["tests"]
